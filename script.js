document.getElementById('cta-btn').addEventListener('click', function () {
  const msg = document.getElementById('click-msg');
  msg.textContent = "You just clicked a button on a live website — nice.";
});
